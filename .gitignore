# 本仓库只发布静态页面，无需忽略额外文件
.DS_Store

# Local Netlify folder
.netlify

# Python（tools/ 下有脚本，跑起来会生成字节码；口径与 fba-rate-parser 一致）
__pycache__/
*.pyc

# M0 利润配置台的舞台目录（2026-09-25）
# 「改配置 → 生成页面副本 → 副本跑闸门 ①~⑪ → 全绿才原子替换正式页面」里的那份副本。
# 它必须落在**本仓库内**（与 index.html 同一文件系统，os.replace 才原子），故必须忽略，
# 否则它会让 git 工作区常驻一条未跟踪记录，干扰「工作区是否干净」的判断。
.m0-stage/
